{
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "socket.io-client": "^4.7.4",
    "eslint": "^8.50.0",
    "@cucumber/cucumber": "^9.0.0",
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint . --ext .js",
    "pretest": "npm run lint --silent || true"
    ,"bdd": "cucumber-js --require features/steps --publish-quiet",
    "pw:test": "playwright test"
  }
}
